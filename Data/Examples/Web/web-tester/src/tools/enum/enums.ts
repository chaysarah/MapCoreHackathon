// dont change the order of the enum!!
// it affects the responsive dialogs width

export enum DialogTypesEnum {
    chooseLayers = 1,
    objectProperties,
    createTexture,
    addJsonViewport,
    scanProperties,
    scanResult,
    objectWorldTree,
    mapWorldTree,
    scanGeometry,
    font,
    editModeProperties,
    editObject,
    initObject,
    checkRenderNeeded,
    eventCallback,
    addMapManuly,
    standAloneSpatialQueries,
    spatialQueries,
    printMap,
};
